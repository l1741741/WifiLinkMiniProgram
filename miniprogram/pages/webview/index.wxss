/* ==========================================================================
 * 网页容器页样式
 * --------------------------------------------------------------------------
 * 没什么可写的：<web-view> 会自己铺满整页，样式由被打开的那个网页决定，
 * 这里不需要干预。
 *
 * ⚠️ 提醒：这个文件曾经被误写成了 JSON（把 index.json 的内容复制了过来），
 *    编译时报的错是：
 *      [ WXSS 文件编译错误] unexpected `"` at pos 5
 *    .wxss 里只能写 CSS。检查脚本现在会校验文件内容与扩展名是否匹配。
 * ========================================================================== */

/* 兜底：万一 web-view 没铺开，至少不让页面出现白边 */
page {
  height: 100%;
}
